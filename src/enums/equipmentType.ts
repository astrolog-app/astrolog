export enum EquipmentType {
  // eslint-disable-next-line no-unused-vars
  TELESCOPE = 'Telescope',
  // eslint-disable-next-line no-unused-vars
  CAMERA = 'Camera',
  // eslint-disable-next-line no-unused-vars
  MOUNT = 'Mount',
  // eslint-disable-next-line no-unused-vars
  FILTER = 'Filter',
  // eslint-disable-next-line no-unused-vars
  FLATTENER = 'Flattener',
}
